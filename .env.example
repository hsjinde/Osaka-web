# Osaka-web 環境變數範本（複製成 .env 填值）
# vault markdown 路徑（本機開發用，CI 中由 workflow 注入）
NOTES_DIR="D:\\大阪-vault"

# Worker API base URL（Task 17 部署後填入；空白時前端走純 localStorage 離線模式）
VITE_API_BASE=https://osaka-dashboard.ethan19980803.workers.dev

# Cloudflare 部署用（Task 17 wrangler deploy）
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=